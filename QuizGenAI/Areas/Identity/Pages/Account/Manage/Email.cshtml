@page
@model EmailModel
@{
    ViewData["Title"] = "Đổi email";
    ViewData["ActivePage"] = "UserSettings";
    ViewData["ManagePage"] = "Email";
}

<div class="mb-8">
    <h1 class="text-h1 font-h1 text-on-surface dark:text-primary-fixed-dim">Cài đặt tài khoản</h1>
    <p class="mt-2 text-body text-secondary dark:text-secondary-fixed-dim">Quản lý thông tin cá nhân và bảo mật tài khoản của bạn.</p>
</div>

<div class="grid grid-cols-1 gap-6 lg:grid-cols-[240px_minmax(0,1fr)]">
    <aside class="h-fit rounded-2xl border border-slate-200 bg-white p-3 shadow-sm dark:border-outline-variant dark:bg-on-secondary-fixed">
        <partial name="_ManageNav" />
    </aside>

    <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-outline-variant dark:bg-on-secondary-fixed md:p-8">
        <partial name="_StatusMessage" for="StatusMessage" />

        <h2 class="text-h2 font-h2 text-on-surface dark:text-primary-fixed-dim">Đổi email đăng nhập</h2>
        <p class="mt-1 text-sm text-secondary dark:text-secondary-fixed-dim">Bạn cần nhập mật khẩu hiện tại để xác nhận thay đổi email.</p>

        <form method="post" class="mt-8 max-w-xl space-y-5">
            <div asp-validation-summary="ModelOnly" class="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700"></div>

            <div>
                <label class="mb-2 block text-sm font-semibold text-on-surface dark:text-on-primary-container">Email hiện tại</label>
                <input value="@Model.CurrentEmail" disabled class="w-full rounded-xl border-slate-200 bg-slate-100 px-4 py-3 text-sm text-secondary dark:border-outline-variant dark:bg-surface-dim/10 dark:text-secondary-fixed-dim" />
            </div>
            <div>
                <label asp-for="Input.NewEmail" class="mb-2 block text-sm font-semibold text-on-surface dark:text-on-primary-container"></label>
                <input asp-for="Input.NewEmail" autocomplete="email" class="w-full rounded-xl border-slate-200 bg-white px-4 py-3 text-sm focus:border-primary focus:ring-primary dark:border-outline-variant dark:bg-on-secondary-fixed" />
                <span asp-validation-for="Input.NewEmail" class="mt-1 block text-sm text-red-600"></span>
            </div>
            <div>
                <label asp-for="Input.CurrentPassword" class="mb-2 block text-sm font-semibold text-on-surface dark:text-on-primary-container"></label>
                <input asp-for="Input.CurrentPassword" autocomplete="current-password" class="w-full rounded-xl border-slate-200 bg-white px-4 py-3 text-sm focus:border-primary focus:ring-primary dark:border-outline-variant dark:bg-on-secondary-fixed" />
                <span asp-validation-for="Input.CurrentPassword" class="mt-1 block text-sm text-red-600"></span>
            </div>

            <button type="submit" class="inline-flex items-center gap-2 rounded-xl bg-primary px-6 py-3 text-sm font-semibold text-white shadow-sm transition hover:bg-primary/90">
                <span class="material-symbols-outlined text-[18px]">mark_email_read</span>
                Đổi email
            </button>
        </form>
    </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
